package com.borqs.ai;

import android.app.Application;
import android.util.Log;

public class AutoInbox extends Application {
    public static final String TAG = "AutoInbox";
    public static final boolean DEBUG = true;    
    public static final long MESSAGE_CHECK_INTERVAL_SECONDS = 10000;
    @Override
    public void onCreate() {
        super.onCreate();
        
        if (DEBUG) {
            Log.d(TAG, "AutoInbox Created");
        }
    }

}
